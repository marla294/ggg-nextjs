import styled from 'styled-components';

const ButtonStyles = styled.button`
  width: 300px;
  height: 40px;
  transition: 0.2s;
  margin: 0 !important;
  padding: 0.7rem 1rem;
`;

export default ButtonStyles;